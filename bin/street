#!/usr/bin/env node

var program = require('commander');
var street = require('../index')

program
  .version('0.1.0')
  .option('-n, --dry-run', 'Dry Run')
  .option('-c, --content-dir', 'Content Directory to upload to S3')
  .parse(process.argv);

console.log(program.dryRun);
